# Cloudflare Super Powers - Fixed Requirements
# 🎯 Memory-optimized deployment for 8GB RAM empire
# ⚡ Fixed dependency conflicts for successful deployment

cloudflare>=2.19.0
requests>=2.31.0
python-dotenv>=1.0.0

# AI/ML dependencies (lightweight versions)
sentence-transformers>=2.2.2
numpy>=1.24.0
torch>=2.0.0,<2.2.0  # Lightweight version

# Optional performance enhancements
psutil>=5.9.0

# Removed problematic dependencies:
# - asyncio-compat (not needed for Python 3.8+)
# - heavy torch versions
# - unnecessary scipy dependencies
